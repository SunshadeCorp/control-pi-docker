options {
    directory "/var/cache/bind";
    listen-on { any; };
    recursion yes;

    forwarders {
        9.9.9.9;
        149.112.112.112;
    };
};

zone "sunshade.local." {
    type primary;
    file "/var/lib/bind/db.sunshade.local";
    notify explicit;
};
